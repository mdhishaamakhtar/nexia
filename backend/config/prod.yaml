server:
  port: 8080
  mode: release
  jwt_secret: "" # MUST be set via NEXIA_SERVER_JWT_SECRET
  jwt_expiry_minutes: 60
  cors_origins: [] # Set via NEXIA_SERVER_CORS_ORIGINS (comma-separated)

db:
  host: ""
  port: 5432
  user: ""
  password: ""
  name: ""
  ssl_mode: "require"

ai:
  gemini_api_key: ""
  redis_url: "127.0.0.1:6379"
