server:
  port: 8080
  mode: release
  jwt_secret: "change-me-in-prod"
  jwt_expiry_minutes: 60
  cors_origins:
    - "http://localhost:3000"

db:
  host: localhost
  port: 5432
  user: postgres
  password: password
  name: nexia_db
  ssl_mode: disable

ai:
  gemini_api_key: ""
  redis_url: "127.0.0.1:6379"
